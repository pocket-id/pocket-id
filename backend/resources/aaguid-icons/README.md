Authenticator icons generated by the `Update AAGUIDs` workflow from the community AAGUID list.

This file keeps the directory populated so that the `go:embed` directive in `resources/files.go` still resolves when no icons are present.
