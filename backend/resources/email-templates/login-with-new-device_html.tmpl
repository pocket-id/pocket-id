{{ define "base" }}
<div class="header">
   <div class="logo">
      <img src="{{ .LogoURL }}" alt="{{ .AppName }}" width="32" height="32" style="width: 32px; height: 32px; max-width: 32px;"/>
      <h1>{{ .AppName }}</h1>
   </div>
   <div class="warning">Waarschuwing</div>
</div>
<div class="content">
   <h2>Nieuwe aanmelding gedetecteerd</h2>
   <table class="grid">
      <tr>
         {{ if and .Data.City .Data.Country }}
         <td>
            <p class="label">Geschatte locatie</p>
            <p>{{ .Data.City }}, {{ .Data.Country }}</p>
         </td>
         {{ end }}
         <td>
            <p class="label">IP-adres</p>
            <p>{{ .Data.IPAddress }}</p>
         </td>
      </tr>
      <tr>
         <td>
            <p class="label">Apparaat</p>
            <p>{{ .Data.Device }}</p>
         </td>
         <td>
            <p class="label">Aanmeldtijd</p>
            <p>{{ .Data.DateTime.Format "2006-01-02 15:04:05 UTC" }}</p>
         </td>
      </tr>
   </table>
   <p class="message">
      Deze aanmelding is gedetecteerd vanaf een nieuw apparaat of locatie. Als je deze activiteit herkent, kun je
      dit bericht veilig negeren. Zo niet, controleer dan je account en beveiligingsinstellingen.
   </p>
</div>
{{ end -}}